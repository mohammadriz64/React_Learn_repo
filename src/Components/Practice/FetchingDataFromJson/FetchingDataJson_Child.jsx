import React from 'react';

export const FetchingDataJson_Child = () => {
    return (
        <div>
            <p>Today will see json data and fetching it using map function.</p>
        </div>
    );
};
